DEFAULT_PAGE=home
DEFAULT_METHOD=get

DB_HOST=db
DB_NAME=phptframework
DB_USER=root
DB_PASS=rootpassword

# SMTP configuration (do NOT commit real credentials)
# SMTP_HOST=codecraft.gr
# SMTP_PORT=465
# SMTP_USER=info@codecraft.gr
# SMTP_PASS=your_smtp_password
# SMTP_SECURE=ssl   # ssl or tls
# MAIL_FROM=info@codecraft.gr
# MAIL_FROM_NAME=phptframework
